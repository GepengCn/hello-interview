`HashMap` 在 Java 中使用长度为 2 的幂次方的数组，主要是为了优化散列值到数组索引的映射过程。当数组的长度是 2 的幂次方时，可以用散列值的低位作为索引位置，这相当于对散列值执行模运算（`hash % array_length`），但比模运算更高效。

具体来说，当数组长度是 2 的幂时，可以通过散列值与数组长度减一的位与操作（`hash & (array_length - 1)`）来得到索引。这个操作等效于模运算，但位与操作比模运算速度更快，从而提高了整体性能。

此外，这种方法还有助于更均匀地分布散列值，减少哈希碰撞，从而提高 `HashMap` 的效率。